---
date: 2026-07-28
pr: pending
feature: Per-user theme customization
impact: Chat and group-chat message text now follows the signed-in user's persisted base font size while retaining the existing message layout and rendering behavior.
---

The theme page also lets each user choose text and accent colors and upload a
background image. Theme metadata is stored per user, while background files are
served separately and cached by the client outside local storage.
